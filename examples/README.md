# Examples

Reference material kept out of the site. Nothing here is published: these files
sit outside `src/content/`, so the posts collection never sees them.

- `posts/` — the starter posts that shipped with the theme, kept as a formatting
  and frontmatter reference while writing real articles.

The two upstream reference posts by Sat Naing stay in
`src/content/posts/` with a `_` prefix instead, which is what excludes them.
